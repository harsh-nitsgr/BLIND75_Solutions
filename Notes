Recurssion: if possible always use reference in the function to recurr oherwise it will create a copy everytime and time-complexity will increase.
String: isalnum(c) --> c is a char and return type is bool if the char is alphanumeric then true else false.
